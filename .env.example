# Copy to .env and fill in real values locally. Never commit .env itself
# (it's gitignored) — for CI, add the same keys as GitHub Actions repo
# secrets instead of reading this file in a workflow.

GMAIL_PW=
